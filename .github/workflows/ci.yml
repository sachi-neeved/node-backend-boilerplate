name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  # Runs first and gates everything else below (except spell-check, which is unrelated
  # to code correctness): a vulnerable dependency should fail CI immediately, before any
  # other job spends runner time — mirroring the pre-commit hook's audit-first ordering.
  dependency-audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.12.0"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Run pnpm audit
        run: pnpm audit --audit-level high

  # Fast, independent checks — no service setup, run in parallel with each other
  # (but only after dependency-audit passes).
  type-check:
    name: Type Check
    needs: [dependency-audit]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.12.0"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: npx tsc --noEmit

  format-lint-check:
    name: Format/Lint Check
    needs: [dependency-audit]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.12.0"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Run biome ci
        run: pnpm check:ci

  unit-tests:
    name: Unit Tests
    needs: [dependency-audit]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.12.0"
          cache: pnpm
      - name: Set up env
        run: |
          cp .env.example .env
          sed -i 's/^DOMAIN=$/DOMAIN=localhost/' .env
          sed -i 's/^ALLOWED_ORIGINS=$/ALLOWED_ORIGINS=http:\/\/localhost:8000/' .env
          sed -i 's/^SMTP_USER=$/SMTP_USER=ci-test/' .env
          sed -i 's/^SMTP_PASS=$/SMTP_PASS=ci-test/' .env
      - run: pnpm install --frozen-lockfile
      - name: Env config check
        run: node -r ts-node/register -r tsconfig-paths/register -e "require('./src/lib/constants')"
      - name: Run unit tests
        run: pnpm test:unit

  # Slow job (real MongoDB startup + app boot) — only runs once the fast correctness
  # checks above have already passed, so a broken build doesn't waste CI minutes here.
  e2e-tests:
    name: E2E Tests
    needs: [type-check, format-lint-check, unit-tests]
    runs-on: ubuntu-latest
    env:
      # Pin mongodb-memory-server's binary download dir explicitly so the cache step
      # below targets a known path instead of guessing the tool's default.
      MONGOMS_DOWNLOAD_DIR: ${{ github.workspace }}/.cache/mongodb-binaries
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.12.0"
          cache: pnpm
      - name: Set up env
        run: |
          cp .env.example .env
          sed -i 's/^DOMAIN=$/DOMAIN=localhost/' .env
          sed -i 's/^ALLOWED_ORIGINS=$/ALLOWED_ORIGINS=http:\/\/localhost:8000/' .env
          sed -i 's/^SMTP_USER=$/SMTP_USER=ci-test/' .env
          sed -i 's/^SMTP_PASS=$/SMTP_PASS=ci-test/' .env
      - run: pnpm install --frozen-lockfile
      - name: Cache mongodb-memory-server binary
        uses: actions/cache@v4
        with:
          path: ${{ env.MONGOMS_DOWNLOAD_DIR }}
          key: mongodb-binaries-${{ runner.os }}
      - name: Run E2E tests
        run: pnpm test:e2e

  # Independent of code correctness — run in parallel, don't gate or get gated by anything.
  spell-check:
    name: Spell Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24.12.0"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Run cspell
        run: pnpm spell
